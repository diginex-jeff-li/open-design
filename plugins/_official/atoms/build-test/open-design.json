{
  "$schema": "https://open-design.ai/schemas/plugin.v1.json",
  "name": "build-test",
  "title": "Build test",
  "version": "0.1.0",
  "description": "Run the project's build / typecheck / lint / test commands and emit build.passing + tests.passing signals.",
  "license": "MIT",
  "tags": ["atom", "first-party", "code-migration", "tune-collab", "phase-7"],
  "compat": { "agentSkills": [{ "path": "./SKILL.md" }] },
  "od": {
    "kind": "atom",
    "scenario": "code-migration",
    "mode": "critique",
    "capabilities": ["prompt:inject", "subprocess", "fs:read"]
  }
}
